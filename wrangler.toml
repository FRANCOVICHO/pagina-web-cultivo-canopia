name = "pagina-web-cultivo-canopia"
main = "worker/index.js"
compatibility_date = "2024-01-01"

# Variables de entorno (valores sensibles via: wrangler secret put NOMBRE)
# GROQ_API_KEY  → wrangler secret put GROQ_API_KEY
# POCKETBASE_URL → wrangler secret put POCKETBASE_URL

[vars]
# Variables no sensibles (pueden ir acá)
ENVIRONMENT = "production"
